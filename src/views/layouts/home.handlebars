<html>
<head>
    <title>Lista de Productos</title>
</head>
<body>
    <h1>Lista de Productos</h1>
    <ul>
        {{#each products}}
            <li>{{this.title}} - Precio: ${{this.price}}</li>
        {{/each}}
    </ul>
</body>
</html>
